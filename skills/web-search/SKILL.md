---
name: web-search
description: Web search and page parsing (DuckDuckGo + fetch)
tools:
  - web.search
  - web.fetch
  - browser.open
---

# Web search

1. `web.search` with a query
2. Optionally `web.fetch` top URLs to parse content
3. `browser.open` if the user wants to see the page
